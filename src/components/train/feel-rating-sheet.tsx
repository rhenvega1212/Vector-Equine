"use client";

/**
 * Feel lives on the ride debrief only. No global redirect — that trapped
 * riders on unanswered sheets and felt like the app was stuck.
 */
export function FeelPendingRedirect() {
  return null;
}
